spring:
  application:
    name: pharmacy-digital-assistant

  datasource:
    url: jdbc:postgresql://localhost:5433/pharmacy_db
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
  # 📧 MAIL CONFIGURATION
  mail:
    host: smtp.gmail.com
    port: 587
    username: laptop.ece1635@gmail.com  # BURAYA KENDİ EMAİLİNİZİ YAZIN
    password: ydhsyfanvlpkopgo    # BURAYA APP PASSWORD'ÜNÜ YAZIN (boşluklar olmadan)
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
    default-encoding: UTF-8

server:
  port: 8080

logging:
  level:
    com.pharmacy.assistant: DEBUG
    org.springframework.web: INFO

  pharmacy:
    email:
      from: laptop.ece1635@gmail.com  # Gönderen email
      from-name: "Eczane Dijital Asistan"
      reply-to: laptop.ece1635@gmail.com